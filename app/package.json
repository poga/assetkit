{
  "name": "asset_kit_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/electron index.js",
    "build": "./node_modules/.bin/electron-packager . SuiSui --platform=darwin --arch=x64 --overwrite"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^7.0.2",
    "electron-prebuilt": "^1.1.1"
  },
  "dependencies": {
    "electron-json-storage": "^2.0.0",
    "rimraf": "^2.5.2"
  }
}
